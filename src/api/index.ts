import * as event from './event';
import * as scrollingText from './scrolling-text';
import * as team from './team';
import * as upcomingEvent from './upcoming-event';

const api = {
  team,
  event,
  scrollingText,
  upcomingEvent,
};

export default api;
