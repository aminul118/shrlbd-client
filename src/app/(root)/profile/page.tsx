import { Metadata } from 'next';

const ProfilePage = () => {
  return <div></div>;
};

export default ProfilePage;

// >> SEO Start
export const metadata: Metadata = {
  title: 'Settings | SHRL',
};
// >> SEO End
